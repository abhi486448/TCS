#include<bits/stdc++.h>
using namespace std;

int main() {
    // int** ptr;
    // cout << ptr <<endl; //" It will throw a garbage value"

    //" But if we want that our pointer does not give any garbage value then we can use "NULL" "
    int** ptr = NULL;

    cout << ptr <<endl;
    return 0;
}